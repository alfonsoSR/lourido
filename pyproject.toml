[build-system]
requires = [
    "hatchling"
]
build-backend = "hatchling.build"


[project]
name = "lourido"
dynamic = [
    "version"
]
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "nastro"
]

[project.optional-dependencies]
test = [
    "pytest"
]
